{
  "config": {
    "step": {
      "user": {
        "title": "Set up Media Index",
        "description": "Configure media folder scanning and indexing.\n\nBase folder must be an actual filesystem path, NOT a media-source:// URI. Use the actual path that media_source maps to (e.g., /media, /config/www/local).\n\nMedia source URI is optional and only needed for custom media path mappings (copy from Home Assistant Media Card path config). Leave empty for standard /media base_folder paths)",
        "data": {
          "base_folder": "Base media folder (filesystem path: /media, /config/www, NOT media-source://)",
          "media_source_uri": "Optional Media Source URI (e.g., media-source://media_source/local/ when using /config/www as base folder)",
          "watched_folders": "Watched folders (comma-separated relative paths)",
          "scan_on_startup": "Rescan on HA restart (catches changes while HA was offline; scans watched folders only when configured, otherwise full scan)",
          "scan_schedule": "Scan schedule",
          "extract_exif": "Extract EXIF metadata",
          "geocode_enabled": "Enable geocoding (GPS to location names)",
          "geocode_precision": "Coordinate precision (2-6 decimal places)",
          "geocode_native_language": "Use native language names (e.g., 日本 instead of Japan)",
          "max_startup_time": "Max startup scan time (seconds)",
          "concurrent_scans": "Concurrent scan workers",
          "auto_install_libmediainfo": "Auto-install libmediainfo for video metadata extraction. During initial setup, Home Assistant restart required after install. When using install_libmediainfo service, integration auto-reloads (no restart needed). If you have multiple Media Index instances, only enable this for one.",
          "scan_without_libmediainfo": "Allow scanning without libmediainfo (images only). Enable this if you do not have video files and want scanning to proceed even when libmediainfo is not installed. Video metadata will not be extracted.",
          "batch_size": "Batch size (files per batch)",
          "cache_max_age_days": "Cache max age (days)",
          "auto_burst_index": "Auto Burst Indexing",
          "burst_time_window_seconds": "Burst Time Window (seconds)",
          "burst_location_tolerance_meters": "Burst GPS Tolerance (meters)",
          "burst_auto_index_interval_hours": "Minimum Hours Between Watcher Re-indexes",
          "burst_index_after_scan": "Re-index Burst Groups After Each Full Scan",
          "auto_cleanup": "Enable automatic cleanup (remove deleted files from database)",
          "cleanup_schedule": "Cleanup schedule",
          "cleanup_time": "Cleanup time (HH:MM, e.g. 02:00)"
        }
      }
    },
    "error": {
      "folder_not_found": "Folder not found. Please check the path and try again."
    },
    "abort": {
      "already_configured": "Media Index is already configured for this folder."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Media Index Options",
        "description": "Configure scanning and performance settings",
        "data": {
          "media_source_uri": "Media source URI (optional, only needed for custom media_dirs mappings)",
          "watched_folders": "Watched folders (comma-separated)",
          "scan_schedule": "Scan schedule",
          "extract_exif": "Extract EXIF metadata",
          "geocode_enabled": "Enable geocoding",
          "geocode_precision": "Coordinate precision (decimal places)",
          "geocode_native_language": "Use native language names (e.g., 日本 instead of Japan)",
          "max_startup_time": "Max startup scan time (seconds)",
          "concurrent_scans": "Concurrent scan workers",
          "auto_install_libmediainfo": "Auto-install libmediainfo (for video metadata extraction). Only enable for one instance if multiple Media Index instances are used.",
          "scan_without_libmediainfo": "Allow scanning without libmediainfo (images only). Enable this if you do not have video files and want scanning to proceed even when libmediainfo is not installed.",
          "batch_size": "Batch size (files per batch)",
          "cache_max_age_days": "Cache max age (days)",
          "scan_on_startup": "Rescan on HA restart (catches changes while HA was offline; scans watched folders only when configured, otherwise full scan)",
          "auto_burst_index": "Auto Burst Indexing",
          "burst_time_window_seconds": "Burst Time Window (seconds)",
          "burst_location_tolerance_meters": "Burst GPS Tolerance (meters)",
          "burst_auto_index_interval_hours": "Minimum Hours Between Watcher Re-indexes",
          "burst_index_after_scan": "Re-index Burst Groups After Each Full Scan",
          "auto_cleanup": "Enable automatic cleanup (remove deleted files from database)",
          "cleanup_schedule": "Cleanup schedule",
          "cleanup_time": "Cleanup time (HH:MM, e.g. 02:00)"
        }
      }
    }
  }
}
